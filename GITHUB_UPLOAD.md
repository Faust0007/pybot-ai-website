# 📤 Пошаговая инструкция: Загрузка сайта на GitHub

## 📁 Файлы уже у вас есть!

Все необходимые файлы находятся в вашей папке:
```
C:\PythonProjects\PyBot AI\
```

## ✅ Какие файлы нужно загрузить на GitHub:

### Обязательные файлы сайта:
- ✅ `index.html` - главная страница
- ✅ `styles.css` - стили
- ✅ `script.js` - JavaScript
- ✅ `README.md` - документация

### Дополнительные файлы (полезные):
- ✅ `netlify.toml` - конфигурация для Netlify
- ✅ `vercel.json` - конфигурация для Vercel
- ✅ `.gitignore` - исключения для Git
- ✅ `DEPLOY.md` - инструкции по деплою
- ✅ `GITHUB_PAGES_SETUP.md` - настройка GitHub Pages

### Файлы, которые НЕ нужно загружать:
- ❌ `server.py` - только для локального запуска (но можно загрузить)
- ❌ `deploy_netlify.py` - только для Netlify CLI (но можно загрузить)
- ❌ `image/` - папка со скриншотами (не нужна для сайта)

## 🚀 Пошаговая инструкция:

### Шаг 1: Откройте терминал в папке проекта

**В Windows:**
1. Откройте PowerShell или Command Prompt
2. Перейдите в папку проекта:
   ```powershell
   cd "C:\PythonProjects\PyBot AI"
   ```

**Или в Cursor/VS Code:**
- Нажмите `Ctrl + ~` (тильда) чтобы открыть терминал
- Терминал уже будет в правильной папке

### Шаг 2: Проверьте, что Git установлен

```bash
git --version
```

Если Git не установлен, скачайте с: https://git-scm.com/download/win

### Шаг 3: Инициализируйте Git репозиторий

```bash
git init
```

Эта команда создаст скрытую папку `.git` в вашей директории.

### Шаг 4: Добавьте все файлы

```bash
git add .
```

Эта команда добавит все файлы из текущей папки (кроме тех, что в `.gitignore`).

**Или добавьте только нужные файлы:**
```bash
git add index.html styles.css script.js README.md netlify.toml vercel.json .gitignore DEPLOY.md GITHUB_PAGES_SETUP.md
```

### Шаг 5: Создайте первый коммит

```bash
git commit -m "Initial commit: PyBot AI website"
```

### Шаг 6: Переименуйте ветку в main (если нужно)

```bash
git branch -M main
```

### Шаг 7: Создайте репозиторий на GitHub

1. Перейдите на https://github.com
2. Нажмите кнопку **"+"** в правом верхнем углу
3. Выберите **"New repository"**
4. Введите название: `pybot-ai-website` (или любое другое)
5. **НЕ** ставьте галочки на "Initialize with README" и других опциях
6. Нажмите **"Create repository"**

### Шаг 8: Подключите локальный репозиторий к GitHub

**Замените `ваш-username` на ваш реальный username GitHub:**

```bash
git remote add origin https://github.com/ваш-username/pybot-ai-website.git
```

**Пример:**
Если ваш username `ivanov`, то команда будет:
```bash
git remote add origin https://github.com/ivanov/pybot-ai-website.git
```

### Шаг 9: Загрузите файлы на GitHub

```bash
git push -u origin main
```

**При первом push GitHub попросит авторизацию:**
- Если используете HTTPS, введите ваш username и Personal Access Token (не пароль!)
- Как создать токен: https://github.com/settings/tokens
- Или используйте GitHub Desktop для упрощения

## 🎯 Полная последовательность команд (копируйте по очереди):

```bash
# 1. Перейдите в папку проекта (если еще не там)
cd "C:\PythonProjects\PyBot AI"

# 2. Инициализируйте Git
git init

# 3. Добавьте файлы
git add .

# 4. Создайте коммит
git commit -m "Initial commit: PyBot AI website"

# 5. Переименуйте ветку
git branch -M main

# 6. Подключите к GitHub (ЗАМЕНИТЕ ваш-username на свой!)
git remote add origin https://github.com/ваш-username/pybot-ai-website.git

# 7. Загрузите на GitHub
git push -u origin main
```

## 🔐 Авторизация в GitHub

### Вариант 1: Personal Access Token (рекомендуется)

1. Перейдите: https://github.com/settings/tokens
2. Нажмите "Generate new token (classic)"
3. Выберите срок действия и права: `repo` (все права репозитория)
4. Скопируйте токен (показывается только один раз!)
5. При `git push` используйте:
   - Username: ваш GitHub username
   - Password: вставьте токен

### Вариант 2: GitHub Desktop (проще для начинающих)

1. Скачайте: https://desktop.github.com/
2. Войдите в аккаунт GitHub
3. Создайте репозиторий через интерфейс
4. Перетащите файлы в приложение
5. Нажмите "Publish repository"

## ✅ После успешной загрузки:

1. Откройте ваш репозиторий на GitHub
2. Перейдите в **Settings → Pages**
3. Настройте как указано в `GITHUB_PAGES_SETUP.md`
4. Через 1-2 минуты сайт будет доступен!

## 🆘 Если что-то пошло не так:

### Ошибка: "remote origin already exists"
```bash
git remote remove origin
git remote add origin https://github.com/ваш-username/pybot-ai-website.git
```

### Ошибка: "failed to push some refs"
```bash
git pull origin main --allow-unrelated-histories
git push -u origin main
```

### Хотите обновить файлы после изменений?
```bash
git add .
git commit -m "Update website"
git push
```

---

**Удачи! 🚀**

