# 🚀 Деплой сервера на Render

Пошаговая инструкция по деплою Telegram сервера на Render для работы с вашим сайтом.

## 📋 Подготовка

Ваш сайт: `https://faust0007.github.io/pybot-ai-website/`

## 🚀 Шаг 1: Создайте аккаунт на Render

1. Перейдите: https://render.com
2. Зарегистрируйтесь через GitHub (рекомендуется)

## 🚀 Шаг 2: Создайте Web Service

1. Нажмите **"New +"** → **"Web Service"**
2. Подключите ваш GitHub репозиторий `pybot-ai-website`
3. Или используйте **"Public Git repository"** и укажите URL вашего репозитория

## 🚀 Шаг 3: Настройте деплой

### Основные настройки:

- **Name:** `pybot-ai-telegram-server` (или любое другое имя)
- **Environment:** `Python 3`
- **Region:** Выберите ближайший (например, Frankfurt)
- **Branch:** `main` (или ваша основная ветка)

### Build & Deploy:

- **Build Command:** 
  ```bash
  pip install -r requirements.txt
  ```

- **Start Command:**
  ```bash
  gunicorn telegram_bot_server:app
  ```

## 🔐 Шаг 4: Добавьте переменные окружения

В разделе **"Environment Variables"** добавьте:

1. **TELEGRAM_BOT_TOKEN**
   - Value: `5622065192:AAH4ofn0NlCNRuUM7l7LsNWSB60N_a-QXqg`

2. **TELEGRAM_CHAT_ID**
   - Value: `1905088256`

3. (Опционально) **PORT**
   - Value: `10000` (Render автоматически устанавливает порт)

## 🚀 Шаг 5: Задеплойте

1. Нажмите **"Create Web Service"**
2. Дождитесь завершения деплоя (2-3 минуты)
3. После успешного деплоя вы получите URL типа:
   ```
   https://pybot-ai-telegram-server.onrender.com
   ```

## ✅ Шаг 6: Проверьте работу

1. Откройте в браузере: `https://ваш-сервер.onrender.com/health`
2. Должно показать:
   ```json
   {
     "status": "ok",
     "bot_configured": true,
     "site_url": "https://faust0007.github.io/pybot-ai-website/"
   }
   ```

## 🔧 Шаг 7: Обновите script.js

1. Откройте `script.js`
2. Найдите строку:
   ```javascript
   const API_URL = 'https://pybot-ai-server.onrender.com/api/submit';
   ```
3. Замените на реальный URL вашего сервера:
   ```javascript
   const API_URL = 'https://ваш-сервер.onrender.com/api/submit';
   ```
4. Сохраните и загрузите на GitHub:
   ```bash
   git add script.js
   git commit -m "Update API URL with Render server"
   git push
   ```

## ⚠️ Важно для Render:

### Бесплатный план имеет ограничения:
- Сервер "засыпает" после 15 минут бездействия
- Первый запрос после простоя может занять 30-50 секунд
- Это нормально для бесплатного плана

### Решения:
1. Используйте платный план ($7/месяц) - сервер всегда активен
2. Или используйте бесплатный план и примите задержку первого запроса
3. Можно настроить "ping" каждые 10 минут, чтобы сервер не засыпал

## 🧪 Тестирование

1. Откройте ваш сайт: https://faust0007.github.io/pybot-ai-website/
2. Заполните форму
3. Проверьте Telegram - должно прийти уведомление
4. Проверьте логи на Render в разделе "Logs"

## 🆘 Решение проблем

### Сервер не запускается:
- Проверьте логи на Render
- Убедитесь, что `requirements.txt` содержит все зависимости
- Проверьте, что переменные окружения установлены

### CORS ошибки:
- Убедитесь, что в `telegram_bot_server.py` указан правильный домен:
  ```python
  ALLOWED_ORIGINS = [
      'https://faust0007.github.io',
      ...
  ]
  ```

### Telegram не получает сообщения:
- Проверьте токен и Chat ID в переменных окружения
- Убедитесь, что бот запущен (напишите боту `/start`)
- Проверьте логи на Render

---

**Готово! Теперь ваш сайт будет отправлять заявки в Telegram! 🎉**

