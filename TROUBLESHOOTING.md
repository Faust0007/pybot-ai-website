# 🔧 Решение проблем с Telegram уведомлениями

## ❌ Заявки не приходят в Telegram

### Шаг 1: Проверьте переменные окружения на Render

1. Откройте ваш сервис на Render: https://dashboard.render.com
2. Перейдите в **"Environment"** → **"Environment Variables"**
3. Убедитесь, что установлены:
   - `TELEGRAM_BOT_TOKEN` = `5622065192:AAH4ofn0NlCNRuUM7l7LsNWSB60N_a-QXqg`
   - `TELEGRAM_CHAT_ID` = `1905088256`
4. Если переменных нет - добавьте их
5. После добавления/изменения - сервер перезапустится автоматически

### Шаг 2: Проверьте логи на Render

1. На Render откройте раздел **"Logs"**
2. При запуске сервера должны быть строки:
   ```
   ✅ TELEGRAM_BOT_TOKEN установлен: 5622065192:AAH4ofn0...
   ✅ TELEGRAM_CHAT_ID установлен: 1905088256
   ```
3. Если видите предупреждения - переменные не установлены

### Шаг 3: Проверьте консоль браузера

1. Откройте ваш сайт: https://faust0007.github.io/pybot-ai-website/
2. Нажмите **F12** (открыть DevTools)
3. Перейдите на вкладку **"Console"**
4. Заполните форму и отправьте
5. Проверьте сообщения в консоли:
   - `📤 Отправка заявки на сервер:` - должен показать URL
   - `📥 Ответ сервера:` - должен показать статус 200
   - Если есть ошибки CORS - будет видно

### Шаг 4: Проверьте работу сервера

Откройте в браузере:
- https://pybot-ai-website.onrender.com/health

Должно показать:
```json
{
  "status": "ok",
  "bot_configured": true,
  "site_url": "https://faust0007.github.io/pybot-ai-website/"
}
```

Если `bot_configured: false` - переменные окружения не установлены!

### Шаг 5: Проверьте логи при отправке заявки

1. На Render откройте **"Logs"**
2. Заполните форму на сайте
3. В логах должны появиться строки:
   ```
   ============================================================
   📥 Получена заявка с сайта
      Origin: https://faust0007.github.io
      Time: ...
   ============================================================
      Имя: ...
      Телефон: ...
   📤 Отправка в Telegram...
   ✅ Сообщение успешно отправлено в Telegram (Message ID: ...)
   ```

## 🔍 Частые проблемы

### Проблема: CORS ошибка в консоли

**Решение:**
- Убедитесь, что в `telegram_bot_server.py` в `ALLOWED_ORIGINS` есть:
  ```python
  'https://faust0007.github.io',
  'https://faust0007.github.io/pybot-ai-website',
  ```
- Перезапустите сервер на Render (Manual Deploy → Clear build cache & deploy)

### Проблема: "Failed to fetch" в консоли

**Причины:**
1. Сервер на Render "заснул" (бесплатный план)
   - Первый запрос после простоя может занять 30-50 секунд
   - Это нормально для бесплатного плана
   
2. Сервер не запущен
   - Проверьте статус на Render
   - Проверьте логи на наличие ошибок

### Проблема: Запрос доходит до сервера, но Telegram не получает

**Проверьте:**
1. Токен бота правильный
2. Chat ID правильный (узнайте у `@userinfobot`)
3. Бот запущен (напишите боту `/start`)
4. В логах Render должна быть ошибка от Telegram API

### Проблема: Переменные окружения не работают

**Решение:**
1. На Render в настройках сервиса
2. Убедитесь, что переменные добавлены в разделе **"Environment Variables"**
3. Имена должны быть точно: `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID`
4. После добавления - сервер перезапустится автоматически
5. Проверьте логи - должны появиться сообщения о том, что переменные установлены

## ✅ Тестирование

### Тест 1: Проверка сервера
```bash
curl https://pybot-ai-website.onrender.com/health
```

### Тест 2: Проверка отправки в Telegram
Используйте `test_telegram.py`:
```bash
python test_telegram.py
```

### Тест 3: Проверка формы на сайте
1. Откройте сайт
2. Откройте консоль (F12)
3. Заполните форму
4. Проверьте сообщения в консоли и логи на Render

## 📞 Если ничего не помогает

1. Проверьте все шаги выше
2. Скопируйте логи с Render
3. Скопируйте ошибки из консоли браузера (F12)
4. Проверьте, что бот работает: напишите боту `/start` в Telegram

---

**Удачи! 🚀**

