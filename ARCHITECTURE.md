# 🏗️ Архитектура проекта PyBot AI

Подробное описание того, как работает приложение.

## 📋 Обзор системы

Проект состоит из двух основных частей:
1. **Фронтенд** - одностраничный сайт (HTML/CSS/JavaScript)
2. **Бэкенд** - сервер для обработки заявок и отправки в Telegram (Python/Flask)

## 🌐 Фронтенд (Клиентская часть)

### Файлы:
- `index.html` - структура страницы
- `styles.css` - стили и дизайн
- `script.js` - логика и взаимодействие

### Как работает:

1. **Загрузка страницы:**
   - Пользователь открывает сайт
   - Загружаются HTML, CSS и JavaScript файлы
   - Страница отображается с анимациями

2. **Работа формы:**
   - Пользователь нажимает кнопку "Заказать услугу"
   - Открывается модальное окно с формой
   - Пользователь заполняет Имя и Телефон

3. **Отправка данных:**
   ```javascript
   // В script.js
   - Валидация данных (проверка заполненности полей)
   - Отправка POST запроса на сервер через fetch API
   - URL: https://pybot-ai-website.onrender.com/api/submit
   - Данные: { name: "...", phone: "..." }
   ```

4. **Обработка ответа:**
   - Если успешно - показывается сообщение "Спасибо за заявку!"
   - Если ошибка - показывается сообщение об ошибке
   - Форма очищается

## 🖥️ Бэкенд (Серверная часть)

### Файлы:
- `telegram_bot_server.py` - Flask сервер
- `requirements.txt` - зависимости Python
- `.env` - переменные окружения (локально)

### Как работает:

1. **Запуск сервера:**
   ```python
   # Сервер запускается на Render
   # Использует gunicorn для production
   gunicorn telegram_bot_server:app
   ```

2. **Получение запроса:**
   ```
   POST /api/submit
   Headers: Content-Type: application/json
   Body: { "name": "...", "phone": "..." }
   Origin: https://faust0007.github.io
   ```

3. **Обработка запроса:**
   ```python
   # В telegram_bot_server.py
   1. Проверка CORS (разрешен ли домен)
   2. Парсинг JSON данных
   3. Валидация (имя и телефон обязательны)
   4. Формирование сообщения для Telegram
   5. Отправка в Telegram через API
   6. Возврат ответа клиенту
   ```

4. **Отправка в Telegram:**
   ```python
   # Использует Telegram Bot API
   POST https://api.telegram.org/bot{TOKEN}/sendMessage
   {
     "chat_id": "1905088256",
     "text": "🔔 Новая заявка...",
     "parse_mode": "HTML"
   }
   ```

## 🔄 Полный поток данных

```
1. Пользователь на сайте
   ↓
2. Заполняет форму (Имя + Телефон)
   ↓
3. Нажимает "Отправить заявку"
   ↓
4. JavaScript (script.js) отправляет POST запрос
   ↓
5. Запрос идет на Render сервер
   https://pybot-ai-website.onrender.com/api/submit
   ↓
6. Flask сервер (telegram_bot_server.py) получает запрос
   ↓
7. Проверка CORS (разрешен ли домен)
   ↓
8. Валидация данных
   ↓
9. Формирование сообщения для Telegram
   ↓
10. Отправка в Telegram через Bot API
    ↓
11. Telegram бот получает сообщение
    ↓
12. Владелец бота видит уведомление
    ↓
13. Сервер возвращает ответ клиенту
    ↓
14. JavaScript показывает сообщение об успехе
```

## 🔐 Безопасность

### CORS (Cross-Origin Resource Sharing):
```python
# Разрешены запросы только с этих доменов:
ALLOWED_ORIGINS = [
    'https://faust0007.github.io',
    'https://faust0007.github.io/pybot-ai-website',
    'http://localhost:8000',  # для тестирования
]
```

### Переменные окружения:
- `TELEGRAM_BOT_TOKEN` - хранится в переменных окружения Render
- `TELEGRAM_CHAT_ID` - хранится в переменных окружения Render
- Не коммитятся в Git (в .gitignore)

## 📡 API Endpoints

### POST /api/submit
**Назначение:** Прием заявок с сайта

**Запрос:**
```json
POST /api/submit
Content-Type: application/json

{
  "name": "Иван Иванов",
  "phone": "+7 (999) 123-45-67"
}
```

**Ответ (успех):**
```json
{
  "success": true,
  "message": "Заявка успешно отправлена!"
}
```

**Ответ (ошибка):**
```json
{
  "success": false,
  "error": "Имя и телефон обязательны для заполнения"
}
```

### GET /health
**Назначение:** Проверка работоспособности сервера

**Ответ:**
```json
{
  "status": "ok",
  "bot_configured": true,
  "site_url": "https://faust0007.github.io/pybot-ai-website/"
}
```

### GET /
**Назначение:** Информация об API

**Ответ:**
```json
{
  "service": "PyBot AI Telegram Notification Server",
  "status": "running",
  "endpoints": {
    "/api/submit": "POST - Отправка заявки с сайта",
    "/health": "GET - Проверка работоспособности"
  }
}
```

## 🗂️ Структура данных

### Форма заявки:
```javascript
{
  name: string,    // Имя клиента
  phone: string    // Номер телефона
}
```

### Сообщение в Telegram:
```html
🔔 <b>Новая заявка с сайта PyBot AI</b>

👤 <b>Имя:</b> {name}
📞 <b>Телефон:</b> {phone}
🕐 <b>Время:</b> {datetime}

---
Сайт: https://faust0007.github.io/pybot-ai-website/
```

## 🔧 Технологии

### Фронтенд:
- HTML5 - структура
- CSS3 - стили, анимации, адаптивность
- JavaScript (ES6+) - логика, fetch API

### Бэкенд:
- Python 3.13
- Flask 3.0.0 - веб-фреймворк
- Flask-CORS 4.0.0 - обработка CORS
- Requests 2.31.0 - HTTP запросы к Telegram API
- python-dotenv 1.0.0 - загрузка переменных окружения
- Gunicorn 21.2.0 - WSGI сервер для production

### Хостинг:
- GitHub Pages - фронтенд
- Render - бэкенд сервер

## 🚀 Деплой

### Фронтенд:
1. Файлы загружаются в GitHub репозиторий
2. GitHub Pages автоматически публикует сайт
3. Сайт доступен по адресу: `https://faust0007.github.io/pybot-ai-website/`

### Бэкенд:
1. Код загружается в GitHub репозиторий
2. Render подключается к репозиторию
3. Render автоматически деплоит при изменениях
4. Сервер доступен по адресу: `https://pybot-ai-website.onrender.com`

## 📝 Логирование

### На сервере:
```python
# Логируются:
- Входящие запросы (Origin, время, данные)
- Результат отправки в Telegram
- Ошибки (если есть)
```

### В браузере (консоль):
```javascript
// Логируются:
- Отправка заявки (URL, данные)
- Ответ сервера (статус, данные)
- Ошибки (если есть)
```

## 🔍 Отладка

### Проверка работы сервера:
```bash
curl https://pybot-ai-website.onrender.com/health
```

### Проверка отправки в Telegram:
```bash
python test_telegram.py
```

### Проверка формы на сайте:
1. Откройте сайт
2. Нажмите F12 (DevTools)
3. Перейдите на вкладку Console
4. Заполните форму
5. Смотрите логи в консоли

## 📚 Дополнительная документация

- `README.md` - общая информация о проекте
- `DEPLOY.md` - инструкции по деплою сайта
- `DEPLOY_SERVER.md` - инструкции по деплою сервера
- `TELEGRAM_SETUP.md` - настройка Telegram уведомлений
- `TROUBLESHOOTING.md` - решение проблем
- `ENV_SETUP.md` - настройка переменных окружения

---

**Версия:** 1.0  
**Последнее обновление:** Ноябрь 2024

