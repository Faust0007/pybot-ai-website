# 🔐 Настройка .env файла

Инструкция по созданию и настройке файла `.env` для Telegram бота.

## 📝 Создание .env файла

1. **Создайте файл `.env` в корне проекта** (там же, где `telegram_bot_server.py`)

2. **Добавьте следующие переменные:**

```env
TELEGRAM_BOT_TOKEN=5622065192:AAH4ofn0NlCNRuUM7l7LsNWSB60N_a-QXqg
TELEGRAM_CHAT_ID=1905088256
PORT=5000
```

## 🔑 Где взять значения:

### TELEGRAM_BOT_TOKEN
- У вас уже есть токен: `5622065192:AAH4ofn0NlCNRuUM7l7LsNWSB60N_a-QXqg`
- Если нужно создать новый, напишите `@BotFather` в Telegram

### TELEGRAM_CHAT_ID
- Узнайте у бота `@userinfobot` в Telegram
- Или напишите вашему боту и откройте:
  ```
  https://api.telegram.org/bot5622065192:AAH4ofn0NlCNRuUM7l7LsNWSB60N_a-QXqg/getUpdates
  ```
- Найдите `"chat":{"id":1905088256}` - это ваш Chat ID

### PORT (опционально)
- Порт для локального запуска
- По умолчанию: `5000`

## ✅ Проверка

После создания `.env` файла:

1. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

2. **Запустите сервер:**
   ```bash
   python telegram_bot_server.py
   ```

3. **Проверьте работу:**
   - Откройте: `http://localhost:5000/health`
   - Должно показать: `{"status": "ok", "bot_configured": true}`

## 🚀 Для деплоя на Render/Railway

На этих платформах `.env` файл не используется напрямую. Вместо этого:

1. **В настройках сервиса** добавьте переменные окружения:
   - `TELEGRAM_BOT_TOKEN` = ваш токен
   - `TELEGRAM_CHAT_ID` = ваш Chat ID

2. **Сервер автоматически подхватит** переменные из окружения

## ⚠️ Важно

- ✅ Файл `.env` уже добавлен в `.gitignore` - он не попадет в Git
- ✅ НЕ коммитьте `.env` файл с реальными токенами
- ✅ Для продакшена используйте переменные окружения на хостинге

## 📋 Пример полного .env файла:

```env
# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=5622065192:AAH4ofn0NlCNRuUM7l7LsNWSB60N_a-QXqg
TELEGRAM_CHAT_ID=1905088256

# Server Configuration
PORT=5000
```

---

**Готово! Теперь сервер будет использовать переменные из .env файла! 🎉**

